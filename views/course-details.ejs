<!DOCTYPE html>
<html>
<head>
  <title><%= course.name %> Details</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #1f4037, #99f2c8);
      min-height: 100vh;
    }

    .course-card {
      border-radius: 15px;
      transition: 0.3s ease;
    }

    .course-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.3);
    }

    .price {
      font-size: 1.5rem;
      font-weight: bold;
      color: #28a745;
    }

    .btn-custom {
      background-color: #1f4037;
      color: white;
      border-radius: 30px;
    }

    .btn-custom:hover {
      background-color: #16302a;
    }
  </style>
</head>

<body>

  <%- include('partials/navbar') %>

  <div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card course-card shadow-lg p-4 text-center" style="width: 450px;">
      
      <h2 class="text-primary mb-3"><%= course.name %></h2>

      <p class="fs-5">
        <strong>Duration:</strong> <%= course.duration %>
      </p>

      <p class="price mb-4">
        ₹<%= course.fees %>
      </p>

      <a href="/course" class="btn btn-custom w-100">
        ← Back to Courses
      </a>

    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
